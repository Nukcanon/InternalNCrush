# 1.1.1 검증 기록

2026-09-24. 소스 7095c4871b9f78d8c9a0f69856c374cd9f128dbd. 실제 공개 상태와 최종 배포 파일 해시는 루트 PUBLICATION_STATUS.json을 기준으로 확인한다. 이전 검증은 TEST_REPORT_V110.md에 보존했다.

## 기능과 실제 이동

로컬 23개 기능 묶음 3,229/3,229, 터치 20/20 통과. 신규 검사에는 설치 미리보기/확정·4단계 포탑·원격 사격 감쇠·미사일·스킬·낙하 19개, 폭탄/스폰/키트/효과음/조준/0초 84개, 설치해체 실제 출구 이동 48개가 포함된다. 출구 검사는 12개 맵에서 공수 교대 양쪽을 검사하며 수비팀은 준비 중, 공격팀은 준비 종료 후 실제 CharacterBody 이동과 충돌을 사용한다. 좌표를 밖으로 옮기는 검사만으로 통과시키지 않는다.

32개 맵의 전체 지형·동적 물체 초기 상태·경로를 미리 굽고 REVISION114로 복원한다. 지형 캐시 저장 전후 충돌 서명을 비교한다. 봇 이동이 아군 전용 경계를 사격 차폐와 혼동해 멈추던 문제를 재현하고 수정했다. 발 IK 이후 애니메이션 덮어쓰기와 네트워크 보행 위상 점프를 수정했다.

## 접속과 Windows

로컬 32개 독립 ENet 참가자 모두 최신 32명 상태 수신. 재접속/방장 재시작, 부활·사격·피격 기록, 움직이는 물체·문·늦은 참가 동기화, 공수 교대 뒤 무작위 순환 통과. 실제 WebRTC 호스트/클라이언트 상태 동기화 및 종료 통과.

로컬 1.1.1 ZIP의 실제 Windows EXE/PCK로 연습장(10명), 봇전투(8명), headless 방장 연결, 창 방장 연결의 4모드 실행을 통과했다. 같은 패키지의 WebRTC 호스트/참가자 연결도 통과했다. 최종 공개 CI ZIP도 별도로 내려받아 동일한 4모드와 패키지 WebRTC를 통과했다. 개인 설정을 저장하지 않았고 검사 전용 포트를 사용했다.

## 그래픽·웹

중간 기본값: 추가 광원/실시간 그림자/MSAA 해제. 높음에서 활성화. 웹은 원본과 격리된 스테이징에서 텍스처 최대512, 썸네일192, 작은 한글 폰트, 캐릭터/맵 시각 메시 단순화를 적용한다. 충돌·경로·연막·전투 신호는 그대로다. 경량화 후 원본/웹 32개 맵의 전체 CollisionShape 형상·전역 변환·레이어 서명을 별도로 비교하여32/32 일치를 확인했다. 캐릭터 인덱스1,613,712→196,800, 맵 인덱스9,876,420→6,832,488. 최종 공개 웹 파일 합계85,274,849bytes(이전 약140.67MB).

IAB 1280×720 실제 7봇+방장 전투에서 60FPS 표시를 확인했다. WebGL 렌더러는 ANGLE/NVIDIA RTX4080SUPER/D3D11이며 오류 로그 없음. 확대된 정찰병/장비 미리보기, 병과6개와 장비탭5개의 한 줄 배치 및 고정 적용/돌아가기 버튼을 확인했다. 내장 브라우저 포인터 고정 제한에는 드래그 조준 대체가 작동했다.

Windows RTX4080SUPER 1280×720 중간 표본: 8인 평균3.35ms/P955.33ms/최대89.91ms, 32인 평균7.32ms/P9512.28ms/최대141.97ms. 이전 높은 품질 표본과 비교해 그리기 호출은725→352/984→575, 화면 삼각형은611,726→153,244/1,965,984→622,231이었다. 품질 단계가 다르므로 동일화질 순수 성능 비교가 아니다. 최고 지연은 해결되지 않았다.

## 남은 범위

실제 내장GPU, 휴대폰/iOS Safari, 다른 PC 사이 WAN, NAS ARM64와 장시간32인 경기는 미검증. 짧은 고성능 GPU 표본은 이 기기들의 성능 보증이 아니다. 종료 때 소수 OpenGL 텍스처 경고와 간헐 프레임 지연은 남아 있다. 공용 로비는 Cloudflare 계정 인증이 없어 아직 미배포이며 실제 기본 URL은 비워 두었다. 상용 수준 아트/경쟁 승률/MMR/완전한 핵 차단을 완료했다고 주장하지 않는다.

## CI 내보내기 복구

첫 Linux 실행35992054517은 기능3,229개·32참가자·재접속·터치·WebRTC·Windows내보내기를 통과했으나, 격리 웹 프로젝트 글꼴 가져오기 중 Godot4.4.1이 `free(): unaligned chunk detected in tcache 2`로 종료했다. 게임 코드 변경 없이 web/prepare_lightweight.py에서 editor/import/use_multiple_threads=false를 설정하고, 이 가져오기를 긴 테스트 전에 먼저 검사하도록 구성했다. 이는 에디터 가져오기에만 적용하며 런타임 스레드 설정을 줄이지 않는다. 설정 근거: https://github.com/godotengine/godot/blob/4.4.1-stable/editor/editor_file_system.cpp#L2964 . 재빌드 소스68936bac37b693ba7f83b2968d5439abe7e95257의 게임 런타임은7095c48과 동일하다.

## 최종 공개 검증

빌드35994021257 성공: 기능3229/3229와 네트워크·터치·WebRTC·Windows/Web 내보내기 완료. Docker/Worker35992058167 성공. 사이트83b7ef4 / Pages35996339880 성공. 공개 웹19파일 크기/SHA256 전부 CI 산출물과 일치하며 공개1.1.1 메뉴·연습장 실행과 브라우저 오류/경고 없음 확인. 공개 Windows ZIP SHA256 bc84d63bfbc1b6ac1ef6fa404a71e449616122462d62b20c2f1df9b9710eb1b4. NAS 익명 다운로드 해시도 일치.
